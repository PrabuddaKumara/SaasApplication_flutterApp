import 'package:flutter/material.dart';

class UserScreenpage extends StatelessWidget {
  const UserScreenpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("UserScreenpage"),
      ),
    );
  }
}